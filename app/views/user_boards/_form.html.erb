<%= form_with(model: user_board, local: true) do |form| %>
  <% if user_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_board.errors.count, "error") %> prohibited this user_board from being saved:</h2>

      <ul>
      <% user_board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p class="user_board_name">New User Board</p>
  <br/>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_area :question %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
